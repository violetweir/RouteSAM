# A2 关闭梯度裁剪：seed2026

唯一优化变量：全局梯度范数裁剪从 1.0 改为关闭。梯度范数仅记录，不缩放梯度；非有限损失或梯度立即停止。

对照：round2_A2_low_lr_two_seeds_20260910/seed2026，Val 最佳 epoch3，Test Dice 0.9011131564。

固定：A2 503张伪标签+8张GT、相同初始化和样本顺序、batch1、10epochs、AdamW、cosine学习率1e-5至1e-7、原图直接1008、colon polyp、全模块LoRA与原损失。

每epoch验证100张并监测固定72张训练标签拟合；按Val Dice选最佳权重，冻结后自动测试100张。主评估使用colon polyp，空文本为补充；256分辨率逐图Dice宏平均。不根据test挑权重。

旧结果只读，所有新权重和预测写入本目录。先独立smoke检查一次无裁剪更新，再重新初始化正式训练。
