# A0 seed2026：50 epochs，无梯度裁剪

按上一轮设置运行：原A0池428张伪标签+8张GT，seed2026，从SAM3-base和相同LoRA初始化重新训练50轮，不续接旧权重。所有训练图等权，每轮每图一次，batch1，共21800次更新。

AdamW，cosine学习率5e-5→5e-7覆盖完整50轮，关闭梯度裁剪；其余LoRA、损失、原图直接1008、colon polyp设置沿用。50轮的cosine衰减速度比10轮慢，前10轮不等于旧10轮训练。

每轮保存权重并验证完整val100，另监测A0固定64伪标签+8GT的标签拟合。训练完成后冻结Val最佳与epoch50权重，分别测试完整test100，有文本为主评估，空文本为补充。指标在256分辨率逐图宏平均，测试不参与选择权重。

本轮沿用最近一轮的“恢复学习率、关闭裁剪”，因此与更早的A0十轮裁剪1.0版本相比，同时改变训练时长和裁剪设置，不能作为单独的时长消融。

原A0图像、标签、数据划分与结果只读复用，新输出独立保存。启动检查通过后重新初始化正式训练；非有限损失或梯度会停止并记录异常。
