# A2 seed2026 恢复学习率、关闭梯度裁剪

本轮：cosine 学习率恢复为 5e-5 → 5e-7，梯度裁剪继续关闭。

对照：
- 同学习率、裁剪1.0：Val最佳epoch1，Test Dice 0.9129324536。
- 低学习率1e-5→1e-7、无裁剪：Val最佳epoch6，Test Dice 0.8942841633。

固定：原A2池503伪标签+8GT、seed2026相同初始化、相同样本顺序、batch1、10epochs、AdamW、全模块LoRA、原损失、原图直接1008、colon polyp。

每epoch验证100张，并监测原固定72张训练标签拟合。仅按Val Dice选最佳权重，冻结后自动预测完整test100；有文本为主评估，空文本为补充诊断，指标为256逐图Dice宏平均。旧结果只读，新结果独立保存。

梯度范数只记录，不缩放梯度；遇到非有限损失或梯度停止。先完成独立启动检查，再重新初始化正式训练。
