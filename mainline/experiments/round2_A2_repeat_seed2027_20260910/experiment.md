# A2随机种子复现实验

旧A2 seed2026的权重、数据与结果全部保留。

1. 复测旧best epoch1的原图1008、有文本test100，检查预测是否重复一致。
2. seed2027从原始SAM3-base重训10epoch，固定同一503伪标签+8GT池、划分、全模块LoRA、batch1、等权、原图1008、cosine 5e-5至5e-7、clip1.0。改变初始化、dropout与每轮打乱的随机种子。
3. 按有文本validation100的Dice256选择best，之后测试有文本与空文本；不按test择优汇报种子，两次结果都保留。

两次训练只能初步检查随机性，不能代替多种子统计。新目录保存所有输出，旧共享图像仅作读取。

旧权重复测已完成：Dice 0.9129324535696501，IoU 0.8579156124505335；100张mask与上次哈希完全一致。旧文件哈希核验未改变。
